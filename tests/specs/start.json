[
  {
    "name": "/start replies with the welcome + main menu",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Reaction Manager — keep channel engagement on target.\n\nTap a button below to onboard, build a bot pool, set rules, or monitor jobs."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "main menu back button re-renders welcome",
    "steps": [
      {
        "send": { "callback": "menu:main" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Reaction Manager — keep channel engagement on target.\n\nTap a button below to onboard, build a bot pool, set rules, or monitor jobs."
            }
          }
        ]
      }
    ]
  }
]
